{
  "name": "mars3d-project-root",
  "version": "0.0.0",
  "private": true,
  "description": "Root package.json to orchestrate frontend/backend dev scripts for Mars3D project",
  "engines": {
    "node": ">=16"
  },
  "scripts": {
    "start": "npm run dev",
    "prestart": "npm --prefix backend install && npm --prefix frontend install",
    "start:backend": "npm --prefix backend run start",
    "frontend:start": "npm --prefix frontend run dev",
    "frontend:build": "npm --prefix frontend run build",
    "frontend:preview": "npm --prefix frontend run preview",
    "backend:start": "npm --prefix backend run dev",
    "dev": "npx concurrently \"npm run frontend:start\" \"npm run backend:start\"",
    "predev": "npm --prefix backend install && npm --prefix frontend install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "postgres": "^3.4.7"
  }
}
